# HQ Documentation Has Moved

The canonical documentation source now lives in [`../docs-site`](../docs-site).

The public documentation site is intended to deploy to:

```text
https://docs.yourhq.ai
```

This directory is kept only as a transition pointer so older links do not fail immediately.
